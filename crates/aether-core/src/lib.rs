// AETHER Terminal — Core Domain Types
// EP-001: Minimal scaffold. EP-002 will populate with domain types.
// INV-1: This crate contains deterministic code only — no LLM, no IO, no HTTP.
// D1: aether-core depends on std + serde-class only.

//! Canonical types, serialization, and edge decomposition math for AETHER Terminal.
//!
//! This crate is the single source of truth for domain types shared across
//! all three planes (client, server, connectors).

// Placeholder — EP-002 will add the full domain model.
// Until then, this empty crate ensures the cargo workspace is functional.
