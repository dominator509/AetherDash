slug = "polymarket"
display_name = "Polymarket"
pack_version = "0.1.0"
capabilities = ["markets", "ticks", "books"]
asset_kinds = ["binary_contract", "categorical_contract"]
jurisdictions = { allowed = [], blocked = ["US"] }

[endpoints]
prod = "https://gamma-api.polymarket.com"

[rate_limits]
rest_per_min = 60
ws_subscriptions = 100

[data_sources]
markets = { rung = "official_api" }
ticks = { rung = "official_api" }
books = { rung = "official_api" }
resolution = { rung = "official_api" }

[freshness]
tick_stale_ms = 5000
book_stale_ms = 2000
