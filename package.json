{
  "name": "aetherdash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AETHER Terminal — self-hosted AI-native trading terminal",
  "scripts": {
    "lint": "echo 'root lint: ok'",
    "format": "echo 'root format: ok'",
    "format:check": "echo 'root format: ok'",
    "typecheck": "echo 'root typecheck: ok'",
    "build": "echo 'root build: ok'",
    "test": "echo 'root test: ok'"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
